# Claude Code Guide

Use `AGENTS.md` as the canonical development guide for this repository.

Do not rely on old Prompt Tower paths such as `src/api`, `src/providers`, `src/services`, or GitHub integration files. The current architecture is `src/core`, `src/app`, and `src/vscode`.

